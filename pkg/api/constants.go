package api

const (
	// Prefixes all keys in the database or either (DATA, INV, TXN)
	DATA        = "DAT"
	INVENTORY   = "INV"
	TRANSACTION = "TXN"

	// Additional key parts
	SPACES   = "SPACES"
	SEGMENTS = "SEGMENTS"
)
