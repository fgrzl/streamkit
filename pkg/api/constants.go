package api

const (
	// DATA prefixes all data entries in the database.
	DATA = "DAT"
	// INVENTORY prefixes inventory entries in the database.
	INVENTORY = "INV"
	// TRANSACTION prefixes transaction entries in the database.
	TRANSACTION = "TXN"

	// SPACES is used as a key component for space-related storage.
	SPACES = "SPACES"
	// SEGMENTS is used as a key component for segment-related storage.
	SEGMENTS = "SEGMENTS"
)
